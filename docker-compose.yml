services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      ALLOWED_ORIGINS: "http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000"
    volumes:
      - ./fluxo.db:/app/fluxo.db
